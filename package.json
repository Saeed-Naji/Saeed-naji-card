{
  "name": "flower-light-site-checks",
  "private": true,
  "version": "87.0.0",
  "devDependencies": {
    "@playwright/test": "1.55.0"
  },
  "scripts": {
    "test:browser": "playwright test --config=playwright.config.mjs",
    "test:static": "node tests/smoke.mjs"
  }
}
